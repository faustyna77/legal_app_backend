import { Layout } from '../../components/layout/Layout'

export function AdminPage() {
  return (
    <Layout
      content={
        <div>
          <h2>Panel administracyjny</h2>
          <p style={{ color: 'var(--color-text-muted)' }}>Dostępny wkrótce.</p>
        </div>
      }
    />
  )
}
